# Dependencias principales con versiones específicas
gspread==5.10.0
google-auth==2.22.0
python-dotenv==1.0.0
pandas==2.2.3
plotly==6.0.1
streamlit==1.44.1
numpy==1.26.3  # Versión compatible con pandas 2.2.3
folium==0.15.0  # Versión actual estable
streamlit-folium==0.15.1  # Versión compatible con streamlit 1.44.1
oauth2client==4.1.3  # Última versión estable

# Dependencias adicionales que podrían ser necesarias
openpyxl==3.1.2  # Para soporte de Excel
pyarrow==14.0.1  # Recomendado para pandas 2.2.x
# Añade cualquier otra dependencia que necesites